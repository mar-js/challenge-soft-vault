import { Sen, Syne } from "next/font/google";

export const syneFont = Syne({ subsets: ["latin"], variable: "--font-syne" });
export const senFont = Sen({ subsets: ["latin"], variable: "--font-sen" });
